name: rebuild-changelog
# DISABLED — was concatenating incomplete/PLACEHOLDER parts over CHANGELOG.md
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
