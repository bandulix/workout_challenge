# Abstract runtime dependencies. Image builds and CI install from
# requirements.lock.txt (compiled from this file) so they stay
# reproducible. After editing this file, regenerate the lock with:
#   python3.12 -m pip install pip-tools
#   pip-compile --output-file=requirements.lock.txt --strip-extras requirements.txt
#
# Django 5.2 is the current LTS (4.2 extended support ended April 2026).
django>=5.2,<6
djangorestframework>=3.15,<3.17
djangorestframework-simplejwt>=5.3,<6
django-cors-headers>=4.3,<5
django-filter>=24.1,<26
django-redis>=5.4,<6
django-celery-beat>=2.6,<3
celery>=5.3,<6
flower>=2.0,<3
redis>=5,<6
gunicorn>=22,<24
psycopg2-binary>=2.9,<3
requests>=2.31
pytz
qrcode[pil]>=7.4
openai>=1.0.0
gevent>=24.2
pywebpush>=2.0.0
py-vapid>=1.9.0
Pillow>=10.0.0
# HEIC/HEIF from iPhone and Galaxy cameras. Wheels bundle libheif.
pillow-heif>=0.16.0
# 0.3.5+ requires Python 3.12 and changed login() to a tokenstore path.
# GHSA-wjhr-76vg-2hvc is dump()-to-disk 0o644; we persist tokens via
# dumps() into an encrypted DB column and never call dump().
garminconnect>=0.3.2,<0.3.3
cryptography>=41.0.0
sentry-sdk[django]>=2.0,<3
sentry-sdk[celery]>=2.0,<3
