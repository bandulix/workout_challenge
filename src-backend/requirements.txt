# Abstract runtime dependencies. Image builds and CI install from
# requirements.lock.txt (compiled from this file) so they stay
# reproducible. After editing this file, regenerate the lock with:
#   python3.11 -m pip install pip-tools
#   pip-compile --output-file=requirements.lock.txt --strip-extras requirements.txt
#
# Django is pinned to 4.2 LTS on purpose: 5.x is a deliberate migration,
# not a surprise from an unpinned `pip install`.
django>=5.2.16,<6
djangorestframework>=3.15,<3.17
djangorestframework-simplejwt>=5.3,<6
django-cors-headers>=4.3,<5
django-filter>=24.1,<26
django-redis>=5.4,<6
django-celery-beat>=2.6,<3
celery>=5.3,<6
flower>=2.0,<3
redis>=5,<6
gunicorn>=22,<24
psycopg2-binary>=2.9,<3
requests>=2.31
pytz
qrcode[pil]>=7.4
openai>=1.0.0
gevent>=24.2
pywebpush>=2.0.0
py-vapid>=1.9.0
Pillow>=10.0.0
garminconnect>=0.3.5
cryptography>=41.0.0
sentry-sdk[django]>=2.0,<3
sentry-sdk[celery]>=2.0,<3
